# 概述

目前cli已经明显进入瓶颈，需要考虑系统地引入provider。

在实验室里把provider和cli的关系完整建模出来。

## 从"约束行为"到"约束结果"

早期思路是约束 AI 的行为（要求 AI 按步骤执行），但这很难。

现在的思路是约束结果——不管 AI 怎么跳步，audit 会基于 Tag-CHANGELOG-Release 的三角关系发现不一致，并按照不可移动、不捏造的原则进行反脆弱修复。

这就把一个不可控的流程问题，变成了一个可控的状态机收敛问题。服务端的作用就是以极高的频率运行这个状态机，把 AI 制造的混乱在后台默默消化掉。
